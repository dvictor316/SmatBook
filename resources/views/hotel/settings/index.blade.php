@extends('layout.mainlayout')

@section('content')
@include('hotel.partials.pms-styles')
<div class="page-wrapper"><div class="content container-fluid"><div class="hotel-type-page hotel-config-page"><div class="hotel-type-header"><div><span class="hotel-type-label"><i class="fe fe-settings"></i> Configuration</span><h2>Hotel property settings</h2><p>Configuration pages use a settings layout, not operational boards or financial ledgers.</p></div><div class="d-flex gap-2 flex-wrap"><a href="{{ route('hotel.setup.step1') }}" class="btn btn-primary">Hotel Setup</a><a href="{{ route('hotel.rooms.index') }}" class="btn btn-outline-primary">Rooms</a><a href="{{ route('hotel.rate_plans.index') }}" class="btn btn-outline-primary">Rate Plans</a></div></div>@if(!$property)<div class="hotel-type-panel"><div class="hotel-type-panel-body"><div class="alert alert-info mb-0">No property configuration found for this branch. Use Hotel Setup to configure your property.</div></div></div>@else<div class="hotel-config-layout"><aside class="hotel-type-panel"><div class="hotel-type-panel-header"><h5 class="mb-0">Setup Areas</h5></div><div class="hotel-type-panel-body hotel-config-list"><div><strong>Property Identity</strong><p class="small text-muted mb-0">Name, code, address, branch.</p></div><div><strong>Operating Times</strong><p class="small text-muted mb-0">Default check-in and checkout times.</p></div><div><strong>Rooms & Rates</strong><p class="small text-muted mb-0">Rooms, room types, and rate plans.</p></div></div></aside><section class="hotel-type-panel"><div class="hotel-type-panel-header"><h5 class="mb-0">Property Profile</h5></div><div class="hotel-type-panel-body"><div class="row g-3"><div class="col-md-6"><label class="text-muted small">Property</label><div class="fw-semibold">{{ $property->name }}</div></div><div class="col-md-6"><label class="text-muted small">Code</label><div class="fw-semibold">{{ $property->code ?: 'N/A' }}</div></div><div class="col-md-6"><label class="text-muted small">Timezone</label><div class="fw-semibold">{{ $property->timezone ?: 'N/A' }}</div></div><div class="col-md-6"><label class="text-muted small">Address</label><div class="fw-semibold">{{ $property->address ?: 'Not set' }}</div></div><div class="col-md-6"><label class="text-muted small">Default Check-In</label><div class="fw-semibold">{{ $property->default_checkin_time ?: 'Not set' }}</div></div><div class="col-md-6"><label class="text-muted small">Default Checkout</label><div class="fw-semibold">{{ $property->default_checkout_time ?: 'Not set' }}</div></div></div></div></section></div>@endif</div></div></div>
@endsection
