@extends('layout.mainlayout')

@section('content')
@include('hotel.partials.pms-styles')
<div class="page-wrapper"><div class="content container-fluid"><div class="hotel-type-page hotel-directory-page"><div class="hotel-type-header"><div><span class="hotel-type-label"><i class="fe fe-users"></i> Data Management</span><h2>Conference & event reservations</h2><p>Event-linked bookings are shown as an event schedule list with organiser and stay dates.</p></div><a href="{{ route('hotel.rooms.calendar') }}" class="btn btn-outline-primary">Room Calendar</a></div><div class="hotel-type-panel"><div class="hotel-type-panel-body table-responsive"><table class="table hotel-type-table align-middle mb-0"><thead><tr><th>Reservation</th><th>Guest/Organizer</th><th>Property</th><th>Arrival</th><th>Departure</th><th>Total</th><th>Status</th></tr></thead><tbody>@forelse($bookings as $event)<tr><td><strong>{{ $event->reservation_number }}</strong></td><td>{{ $event->customer?->customer_name ?? $event->customer?->name ?? 'N/A' }}</td><td>{{ $event->property?->name ?? 'N/A' }}</td><td>{{ optional($event->arrival_date)->format('d M Y') }}</td><td>{{ optional($event->departure_date)->format('d M Y') }}</td><td>{{ number_format((float) ($event->total ?? 0), 2) }}</td><td><span class="hotel-status-chip gold">{{ ucfirst(str_replace('_',' ', (string)$event->status)) }}</span></td></tr>@empty<tr><td colspan="7" class="text-muted">No event-linked reservations found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $bookings->links() }}</div></div></div></div>
@endsection
